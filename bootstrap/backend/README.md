# backend bootstrap

**Status:** not yet implemented — decision pending, see ADR-0002.

Only needed if ADR-0002 resolves to the self-managed S3+DynamoDB option instead of HCP Terraform Cloud workspaces. Applied once, by hand, per AWS account — this is one of the explicitly-allowed manual steps (requirements §2.1).

See `docs/decisions/ADR-0002-terraform-state.md`.
