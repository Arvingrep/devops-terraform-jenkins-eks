# GitHub OIDC bootstrap

**Status:** not yet implemented — blocked on ADR-0003.

Creates the GitHub OIDC identity provider + `WCDTerraformLabRole`/`WCDTerraformProdPlanRole`/`WCDTerraformProdApplyRole` IAM roles. Blocked on the Jenkins-vs-GitHub-Actions CI decision noted in that ADR.

See `docs/decisions/ADR-0003-github-oidc.md`.
