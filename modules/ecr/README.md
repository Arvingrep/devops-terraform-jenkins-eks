# ecr module

**Status:** not yet implemented.

Container image repositories for workloads deployed onto the EKS module. Not required for the current nginx smoke-test workload; scaffolded for when a real application is containerized.

See `docs/migration-plan.md` for when this is populated and `docs/target-architecture.md` for its intended interface.
