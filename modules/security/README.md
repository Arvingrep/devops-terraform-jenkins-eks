# security module

**Status:** not yet implemented.

Shared security-group / baseline security primitives used by other modules (no `0.0.0.0/0` SSH/HTTP defaults). Landed alongside Migration Plan Phase 3.

See `docs/migration-plan.md` for when this is populated and `docs/target-architecture.md` for its intended interface.
