# iam module

**Status:** not yet implemented.

Least-privilege IAM roles/policies for Jenkins, EKS, and CI (OIDC-federated roles per ADR-0003). Landed alongside Migration Plan Phase 3/4.

See `docs/migration-plan.md` for when this is populated and `docs/target-architecture.md` for its intended interface.
