# observability module

**Status:** not yet implemented.

CloudWatch log groups/alarms and metrics baseline for Jenkins + EKS. Deferred past the first working EKS module per requirements §8.3.

See `docs/migration-plan.md` for when this is populated and `docs/target-architecture.md` for its intended interface.
