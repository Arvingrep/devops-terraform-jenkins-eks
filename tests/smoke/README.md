# smoke tests

**Status:** not yet implemented.

Post-apply smoke checks (Jenkins reachable, EKS API reachable, node Ready) invoked by `scripts/smoke-test.sh` from the future `lab-apply.yml` workflow. Empty until Migration Plan Phase 3+.
