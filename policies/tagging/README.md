# tagging policy

**Status:** not yet implemented.

Machine-checkable definition of the mandatory tag set from requirements §5.3 (`Project`, `Environment`, `ManagedBy`, `Repository`, `Owner`, `CostCenter`). Populated alongside the first module that creates real resources.
