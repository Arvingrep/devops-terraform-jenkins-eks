# Intentionally empty.
#
# Module calls (network, jenkins, eks) are added here incrementally as
# docs/migration-plan.md phases land. Production apply is human-approved
# only (requirements §10.4) and is never run by an automated agent.
