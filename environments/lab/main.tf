# Intentionally empty.
#
# Module calls (network, jenkins, eks) are added here incrementally as
# docs/migration-plan.md phases land. Keeping this file empty now — rather
# than pre-wiring module blocks that don't exist yet — avoids a
# `terraform init` that fails on missing module sources.
